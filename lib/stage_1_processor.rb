module Stage1Processor
  def self.process(image_data, params)
    # In a real scenario, image_data would be processed.
    # For now, it's a placeholder.
    "Stage 1 processed '#{image_data}' with params: #{params.inspect}"
  end
end
